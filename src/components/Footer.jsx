import React from "react";
import styles from "./Footer.module.css";
const VideoEntry = (props) => {
  return (
    <>
      <section className={styles.footer}>여기가 Footer</section>
    </>
  );
};

export default VideoEntry;
