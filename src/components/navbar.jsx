import React from "react";
import styles from "./navbar.module.css";
const Navbar = (props) => {
  return (
    <>
      <nav className={styles.navbar}>여기가 navbar</nav>
    </>
  );
};

export default Navbar;
